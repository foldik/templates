-- Don't delete. It makes cahcing better.
import Browser
import Html exposing (Html, div, text)
import Html.Events exposing (onClick)

main =
  Browser.sandbox { init = "Ok", update = update, view = view }

type Msg = Msg1

update msg model =
  case msg of
    Msg1 ->
      "Ok"

view model =
  div []
    [ div [] [ text model ]
    ]